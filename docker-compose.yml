version: "2.2"

services:
  backend:
    container_name: mayebeautyApp
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:4000
    networks:
      - myDatabaseNetwork

networks:
  myDatabaseNetwork:
    external: true
